apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: wikijs-postgres-pvc
  namespace: wikijs
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 20Gi
